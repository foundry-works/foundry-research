{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Stop",
                "hooks": [
                    {
                        "type": "command",
                        "command": "FILE=$(cat - | python3 -c \"import sys,json; print(json.load(sys.stdin).get('tool_input',{}).get('file_path',''))\" 2>/dev/null); [ -n \"$FILE\" ] && [[ \"$FILE\" == *.py ]] && { ruff check --fix \"$FILE\" 2>&1; pyright \"$FILE\" 2>&1 | grep -E 'error:|warning:' | head -5; } || true",
                        "timeout": 30
                    }
                ]
            }
        ]
    }
}
